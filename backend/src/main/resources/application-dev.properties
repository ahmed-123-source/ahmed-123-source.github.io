spring.datasource.url=jdbc:h2:mem:portfolio;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
# CORS (adjust frontend origin later)
app.cors.allowed-origins=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003,http://localhost:3004
spring.main.allow-bean-definition-overriding=true

# Mail (dev): create a JavaMailSender bean without actually sending
spring.mail.host=smtp.gmail.com
# Optional: if you use a local SMTP catcher like MailHog, set its port
# spring.mail.port=1025

# Contact recipient used by ContactService in dev
app.contact.to=ahmed.charfeddine216@gmail.com

# Disable actual mail send in dev (service will log instead)
app.mail.enabled=true

# Set From address to the SMTP username (recommended when enabling real SMTP)
app.mail.from=${spring.mail.username}

# Example Gmail SMTP (requires an App Password; normal account password will NOT work)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=ahmed.charfeddine216@gmail.com
spring.mail.password=mwcq nmcu dpnr rvxv
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
